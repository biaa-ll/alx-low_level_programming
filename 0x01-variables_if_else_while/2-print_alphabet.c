#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success/correct)
 *
 */
int main(void)
{
char alphabet;

for (aplabet = 'a'; alphabets <= 'z'; alphabet++)
{
putchar(alphabet);
}
putchar('\n');
return (0);
}
